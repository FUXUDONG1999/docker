version: "3"
services:
  redis:
    image: 'redis'
    ports:
      - '6379:6379'
    volumes:
      - D:\program\docker\data\redis\data:/data
      - D:\program\docker\data\redis\conf:/usr/local/etc/redis/
    command: redis-server /usr/local/etc/redis/redis.conf